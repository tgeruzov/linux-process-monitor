# Linux Process Monitor

Простой и надежный Bash-скрипт для мониторинга процесса `test` в среде Linux.

Скрипт автоматически проверяет статус процесса, отправляет heartbeat-запросы на API и логирует случаи перезапуска или недоступности сервера мониторинга.

## Возможности

- 🕒 **Периодичность:** Проверка статуса каждую минуту.
- 🚀 **Автозапуск:** Интеграция с Systemd для запуска при старте системы.
- 📡 **Heartbeat:** Отправка HTTPS-запросов, если процесс жив.
- 📝 **Логирование:**
  - Запись событий перезапуска процесса (сравнение PID).
  - Запись ошибок подключения к серверу мониторинга.

## Требования

- OS: Linux
- Права root (для записи логов в `/var/log` и управления systemd)
- Утилиты: `curl`, `pgrep`

## Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone [https://github.com/ВАШ_НИК/linux-process-monitor.git](https://github.com/tgeruzov/linux-process-monitor.git)
   cd linux-process-monitor
